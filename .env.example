# Copy to .env and set your key(s)
OPENAI_API_KEY=sk-REPLACE_ME
# Optional; defaults to gpt-5
OPENAI_MODEL=gpt-5
# Use 1 to allow repo-local runs/workdir overrides via CLI flags
CEDAR_ALLOW_OVERRIDE=0
# Optional OpenAI API base (for proxies or Azure; leave unset for api.openai.com)
# OPENAI_BASE=https://api.openai.com
# If using Azure OpenAI, set these and OPENAI_MODEL to your deployment name
# AZURE_OPENAI_ENDPOINT=
# AZURE_OPENAI_API_VERSION=2024-10-21

# To fetch and cache the provider key locally (direct OpenAI calls in the app), set:
# CEDAR_KEY_URL=https://cedar-notebook.onrender.com/v1/key
# APP_SHARED_TOKEN=403-298-09345-023495
# CEDAR_REFRESH_KEY=0  # set to 1 to force re-fetch at next run
